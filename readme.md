# Exploiting the Final Component of Generator Architectures for AI-Generated Image Detection

Research Paper Link - [Here](https://arxiv.org/pdf/2601.20461)

Some important points

1. Clone the Janus repository using the following command: `!git clone https://github.com/deepseek-ai/Janus.git`
    Navigate to Janus folder and run command `!pip install -e .`. 
* Ensure that the **transformers** library is installed with version `4.49.0`

2. Clone the PixelFlow repository to access the required functions `!git clone https://github.com/ShoufaChen/PixelFlow`

3. Clone the DinoV3 repository to access the required function `!git clone https://github.com/facebookresearch/dinov3.git`

4. create a empty folder output